plugins {
    id 'java-conventions'
    id 'spring-conventions'
    id 'publishing-conventions'
}

version = '1.0.0'

dependencies {
    api project(':core:util')
    compileOnly "com.google.firebase:firebase-admin:${firebaseAdminVersion}"
}